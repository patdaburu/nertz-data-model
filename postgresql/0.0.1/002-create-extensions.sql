

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- To install PostGIS on Ubuntu, you'll need to install the scripts first.
-- sudo apt-get install postgresql-9.6-postgis-scripts
CREATE EXTENSION IF NOT EXISTS postgis;